#include <iostream>

int main()[
	//Arreglo de enteros con tamaño fijo de 5 elementos
	int numeros[5] = [10, 20, 30, 40, 50];
	
	//Accediendo a los elemntos del arreglo
	for (int i = 0; i < 5; i++) {
		std::cout << "Elementos en la posicion " << i << ":" << numeros[i] << std::end1;
	}

	return 0;
]
